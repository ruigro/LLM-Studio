# Requirements for the installer GUI and pip worker
# This is installed in bootstrap\.venv, NOT in LLM\.venv
# The installer GUI must run from bootstrap to avoid file locks

# GUI framework (installer uses tkinter, but we need PySide6 for the main app check)
PySide6==6.8.1
PySide6-Essentials==6.8.1
PySide6-Addons==6.8.1
shiboken6==6.8.1

# System utilities
psutil>=5.9.0

# Packaging utilities
packaging>=21.0


{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OWLLM Tool Call Schema",
  "description": "Strict JSON envelope for tool calls (PHASE 4: single format only)",
  "type": "object",
  "required": ["tool", "args", "id"],
  "properties": {
    "tool": {
      "type": "string",
      "description": "Name of the tool to call",
      "minLength": 1,
      "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
      "examples": ["calculator", "read_file", "web_search"]
    },
    "args": {
      "type": "object",
      "description": "Arguments to pass to the tool (tool-specific schema)",
      "additionalProperties": true
    },
    "id": {
      "type": "string",
      "description": "Unique identifier for this tool call",
      "minLength": 1,
      "pattern": "^[a-zA-Z0-9_-]+$",
      "examples": ["call_1", "tool_abc123"]
    }
  },
  "additionalProperties": false
}

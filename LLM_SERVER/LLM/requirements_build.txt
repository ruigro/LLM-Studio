# Build requirements for creating standalone installers
# Install these before building: pip install -r requirements_build.txt

# PyInstaller for creating executables
pyinstaller>=6.0.0

# For Windows installer (NSIS)
# Note: NSIS must be installed separately from https://nsis.sourceforge.io/
# This is just a placeholder - NSIS is not a Python package

# Additional build tools (optional)
setuptools>=65.0.0
wheel>=0.40.0

# For downloading Visual C++ Redistributables (Windows)
# urllib is built-in, but requests is more reliable
requests>=2.31.0

